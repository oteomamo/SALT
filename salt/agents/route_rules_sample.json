{
  "version": "salt-route-rules/1",
  "rules": [
    {
      "id": "one-kind-of-helper",
      "when": "worker_kinds < 2",
      "then": {"max_pieces": 1},
      "expected": "with every ready helper described the same way, a plan hands the whole task to one of them anyway, so paying for a fan-out buys nothing",
      "example": true
    },
    {
      "id": "short-ask",
      "when": "ask_words < 8 and ask_list_items == 0",
      "then": {"plan": false},
      "expected": "a line this short is a reply, not a piece of work to split up, and planning it costs three model calls to say so",
      "example": true
    },
    {
      "id": "several-deliverables",
      "when": "ask_list_items > 2 and n_workers_ready > 1",
      "then": {"plan": true},
      "expected": "somebody who wants three things tends to write three lines, and three lines is what a fan-out is for",
      "example": true
    },
    {
      "id": "last-round-went-nowhere",
      "when": "last_round_answered == 0 and turns_since_round <= 1",
      "then": {"plan": false},
      "expected": "the helpers answered nothing at all last turn, so the next turn is worth taking plainly before trying them again",
      "evidence": "reads a signal that routing itself moves, and is qualified by turns_since_round so it cannot freeze: one plain turn later it stops applying",
      "example": true
    }
  ]
}
